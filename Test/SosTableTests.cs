// © Copyright 2016 HP Development Company, L.P.
// SPDX-License-Identifier: MIT
using System;
using System.Text;
using System.Collections.Generic;
using System.Linq;
using Microsoft.VisualStudio.TestTools.UnitTesting;

using TestClasses;

namespace More.Net
{
    [TestClass]
    public class TableTests
    {
        /*
        [TestMethod]
        public void TestTables()
        {
            StringBuilder builder = new StringBuilder();

            Util.TestDeserializationToNull(builder, typeof(BooleanAndInt[]), "<>");
            Util.TestDeserializationToNull(builder, typeof(BooleanAndInt[]), "<:>");
            Util.TestDeserializationToNull(builder, typeof(BooleanWrapper[]), "<b>");
            Util.TestDeserializationToNull(builder, typeof(BooleanWrapper[]), "<b:>");
            Util.TestDeserializationToNull(builder, typeof(BooleanAndInt[]), "<b,i>");
            Util.TestDeserializationToNull(builder, typeof(BooleanAndInt[]), "<b,i:>");

            Util.TestDeserialization(builder, new BooleanAndInt[] { new BooleanAndInt(false, 0), new BooleanAndInt(true, 32) },
                "<b,i:false,0:true,32>");
        }

        [TestMethod]
        public void TestBadTables()
        {
            StringBuilder builder = new StringBuilder();

            TestBadTable(builder, "<");
            TestBadTable(builder, "<:");
            TestBadTable(builder, "<:");
        }
        public void TestBadTable(StringBuilder builder, String badSos)
        {
        }
        */
    }
}
